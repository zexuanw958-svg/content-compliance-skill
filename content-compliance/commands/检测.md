---
description: "中文入口：使用 content-compliance skill 做抖音/小红书内容合规检测"
---

Use the `content-compliance` skill.

Treat this command as the Chinese slash-command alias `/检测`.

If the user did not provide a platform, ask:

```text
老板，这次要按抖音还是小红书来审？
```

Then follow `../SKILL.md` exactly.
